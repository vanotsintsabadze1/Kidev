name: CI

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - run: dotnet restore Kidev.slnx
      - run: dotnet format Kidev.slnx --verify-no-changes --no-restore --exclude Kidev.Storage.PostgreSQL/Migrations
      - run: dotnet build Kidev.slnx --configuration Release --no-restore
      - run: dotnet test Kidev.slnx --configuration Release --no-build
